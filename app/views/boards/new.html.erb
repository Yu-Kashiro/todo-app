<div class=container>
  <%= form_with(model: @board, url: boards_path, method: 'post', local: true) do |f| %>
    <%= f.label :title, 'title' %>
    <%= f.text_field :title %>
    <%= f.label :content, 'content' %>
    <%= f.text_area :content %>
    <%= f.submit "Submit" %>
  <% end %>
</div>